const sliceArray = ["red", "orange", "yellow", "green", "blue", "indigo", "violet"]
const sliced = sliceArray.slice(2,5) // start at the beginning, get items up to index 3
const lastItems = sliceArray.slice(-4)
console.log(sliced)
console.log(sliceArray)
console.log(lastItems)